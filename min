#include<stdio.h>
int main()
{
  int i,min,a[5]={60,90,87,67,45},n;
  n=sizeof(a)/sizeof(a[0]);
  min=a[0];
  for(i=0;i<n;i++)
  {
    if(a[i]<min)
    {
      min=a[i];
    }
  }
  printf("the minimum is %d",min);
  return 0;
}
