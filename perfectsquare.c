#include<stdio.h>
#include<math.h>
 int main()
 {
 	int i,number =46;
  for(i = 0; i <= number; i++)
    {
        if(number == i*i)
        {
            printf("\n\n\n\t\t\t%d is a perfect square\n\n\n", number);
        }
	} 
	      
        printf("the number is not a perfect square");

    return 0;
}
